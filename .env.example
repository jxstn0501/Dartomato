# ParseExtract API Configuration
PARSEXTRACT_URL=https://api.parseextract.com/v1/data-extract
PARSEXTRACT_API_KEY=your_api_key_here

# Optional: Custom extraction prompt
# PARSEXTRACT_PROMPT=Extract all dart game scores, player names, and round information from this image. Format as JSON with fields: rounds, scores, players.

# Optional: Enable stub mode for testing without API calls
# PARSEXTRACT_STUB=1

# Flask session secret (change in production)
SESSION_SECRET=dev-secret-key-change-in-production

# Optional: Set host and port
HOST=0.0.0.0
PORT=5000
